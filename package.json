{
  "name": "rename",
  "version": "0.0.0",
  "main": "server.ts",
  "scripts": {
    "start": "tsc && node server.js",
    "prebuild": "tsc",
    "deploy": "sqwelch deploy"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/node": "^16.4.13"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "sequelize": "^6.6.5"
  }
}
